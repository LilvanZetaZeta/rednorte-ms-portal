services:
  app:
    build: .
    container_name: ms-portal-app
    ports:
      - "8082:8082"
    env_file:
      - .env
    networks:
      - network-RedNorte

networks:
  network-RedNorte:
    external: true
